import { styled } from "@mui/material/styles";
import Box from "@mui/material/Box";

export const BoxSearchBar = styled(Box)(({ theme }) => ({
  backgroundColor: "#F9FAFC",
  padding: 2,
  borderRadius: 2,
  display: "flex",
  alignItems: "center",
  boxShadow: "0px 2px 4px rgba(0, 0, 0, 0.1)"
}));
